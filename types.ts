
export interface DriverStats {
  grandsPrix: number;
  points: number;
  podiums: number;
  number: string;
  name: string;
  location: string;
}

export interface RaceCountdown {
  days: number;
  hours: number;
  minutes: number;
}
